# UAT Environment Configuration (Neon PostgreSQL)
server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:ep-shy-pine-adl72xly-pooler.c-2.us-east-1.aws.neon.tech}/${DB_NAME:neondb}?sslmode=require
spring.datasource.username=${DB_USER:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_bsyp0tC5iSGe}
spring.datasource.driverClassName=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# SQL Init (Force structure creation via schema.sql)
spring.sql.init.mode=always

# Logging
logging.level.com.abtech.backend=INFO
